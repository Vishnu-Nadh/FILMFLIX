import React from "react";
import styles from "./LoginScreen.module.css";

const LoginScreen = () => {
  return (
    <main className={`${styles.login} auth-bg`}>
      {/*  */}
      {/*  */}
    </main>
  );
};

export default LoginScreen;
